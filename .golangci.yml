version: "2"

run:
  timeout: 5m
  go: "1.26.1"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell
    - nilerr
    - gosec
    - bodyclose
    - copyloopvar
